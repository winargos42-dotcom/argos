name: Deploy to Railway
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm Railway source deployment
        run: |
          echo "Railway deploys this repository from its connected GitHub source."
          echo "Commit: ${GITHUB_SHA}"
