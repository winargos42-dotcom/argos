FROM python:3.11-slim
WORKDIR /app
COPY scripts/gcp_gemini_proxy/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY scripts/gcp_gemini_proxy/main.py .
EXPOSE 8080
ENV PORT=8080
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8080"]
