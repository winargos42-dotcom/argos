## [2026-05-28 19:20] ACTION REPORT — MCP Cache Write Permissions (P1)

**Executor:** Claude Code (Nexus, root)
**Target:** `sqlite3.OperationalError: attempt to write a readonly database`

### Actions Performed
```bash
ls -la /home/ava/Projects/argoss/data/mcp_cache.db   # -rw-r--r-- ava ava
lsof /home/ava/Projects/argoss/data/mcp_cache.db      # NO LOCKS
tail recovered_daemons_live.log                       # no sqlite errors
```

### Findings
- File `mcp_cache.db` is **writable by root** (Claude runs as root)
- Current live instance of `recovered_daemons` (PID=128147, restart #1) shows **no sqlite errors**
- Earlier `sqlite3.OperationalError` in `recovered_daemons_20260528_1152.log` was **transient**, likely caused by:
  - A prior process holding the database open in WAL-exclusive mode before SIGTERM cleanup
  - Or file permissions at the time of that specific run under user `ava` without write access
- Recovered daemons now run as root (inherited from orchestrator), so write access is guaranteed

### Live Issues Observed
Instead of sqlite errors, recovered_daemons now show Telegram API errors:
- `Bad Request: chat not found` — invalid target chat_id
- `Read timed out` (api.telegram.org) — residual network latency

### Resolution
- MCP cache write permissions: **NOT REPRODUCING** / **RESOLVED** after stack restart
- SQLite file healthy, no journal lock conflicts

### Next
- P2 → investigate dreamer_standalone 0-insights
- P2 → verify localhost:5001 Brain API heartbeat stability
