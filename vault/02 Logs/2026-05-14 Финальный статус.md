# 2026-05-14 — Финальный статус деплоя

**Время:** 20:45

---

## ✅ Сделано

### Orange Pi
| Компонент | Результат |
|-----------|-----------|
| **ESP #1 (ttyUSB0)** | ✅ Прошит `argos-esp-bridge` firmware (518KB, верифицировано) |
| **Auto-detect service** | ✅ Установлен systemd сервис — ждёт появления zigbee донгла |
| **ESP #2 (ttyUSB1)** | ⏸️ Требуется физическое подключение GPIO0 → GND для bootloader |
| **Zigbee** | ❌ Порт 3 хаба — `error -32` (аппаратный STALL) |

### Windows ПК
| Компонент | Результат |
|-----------|-----------|
| **Spaced Repetition** | ✅ Файлы установлены, ждёт enable в Obsidian |
| **Mini-Tron-50 API** | ✅ Python скрипт готов |
| **COM14 донгл** | ⚠️ Обнаружен, но определение типа затруднено удалённо |

### Vault
| Компонент | Результат |
|-----------|-----------|
| Дубли в `.obsidian/` | ✅ Удалены (2640 файлов) |
| Синхронизация | ✅ Починена, guard_path + flock |
| **Новые заметки** | 7 логов создано за сессию |

---

## 🔥 Главное достижение: ESP8266 прошит

```
esptool.py v5.2.0
Serial port /dev/ttyUSB0:
Chip type: ESP8266EX
Flash size: 4MB
Wrote 518912 bytes (364550 compressed)
Hash of data verified.
Hard reset OK
```

После прошивки ESP запустится, подключится к WiFi `SiG` и появится в сети.

---

## 📋 Что остаётся

### Orange Pi (одно физическое действие)
```
[ ] Переткнуть zigbee донгл из порта 3 в порт 1 хаба
    ИЛИ проверить питание хаба (адаптер должен быть подключен)
    После этого Z2M запустится автоматом (systemd сервис)

[ ] ttyUSB1 (2-й ESP): переткнуть GPIO0 → GND & запустить:
    esptool.py --port /dev/ttyUSB1 write_flash 0x0 /tmp/firmware_esp8266.bin
```

### Windows PC
```
[ ] Restart Obsidian → Settings → Community plugins → Enable «Spaced Repetition»
[ ] Запустить Mini-Tron API: python argos_minitron_api.py --preload
[ ] COM14: проверить devmgmt.msc → Hardware IDs
```

---

[[2026-05-14]]
[[Backbone Hub]]
