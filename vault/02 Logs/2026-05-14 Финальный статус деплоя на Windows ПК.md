# 2026-05-14 — Финальный статус деплоя на Windows ПК (argos-pc)

**Время:** 19:20  
**PC:** DESKTO (192.168.1.66)  
**Агент:** pi-coding-agent

---

## ✅ Сделано

### 1. Vault Cleanup — дубли удалены
- Удалена полная копия vault'а из `.obsidian/` (2640 файлов)
- Сохранены только системные: `plugins/` + `snippets/`

### 2. Синхронизация починена
- `sync-obsidian-memory.py`: `exclude = {".obsidian", ".git"}`
- `vault-sync.sh` v2: `guard_path()` + `flock` lockfile

### 3. Spaced Repetition — установлен на Windows
**Путь:** `F:\debug\аргос\.obsidian\plugins\obsidian-spaced-repetition\`
- `main.js` (2.4 MB)
- `manifest.json`
- `styles.css`
- **Следующий шаг:** Restart Obsidian → Settings → Community plugins → Enable «Spaced Repetition»

### 4. Mini-Tron-50 — модель на Windows
**Путь:** `C:\Users\AvA\Downloads\argos_scripts\mini-tron-50-model\`
- `pytorch_model.bin` (129 MB) ✅
- `config.json`, `tokenizer.model`, `chat.py` ✅
- `Modelfile` ✅ (создан)

---

## 🔄 В процессе / Ожидает действий

### GGUF конвертация (нужна для Ollama)
Модель `mini-tron-50` — архитектура NanoGPT (GPT-2). Требуется конвертация PyTorch → GGUF через `llama.cpp/convert_hf_to_gguf.py`.

**Варианты:**

**A) WSL на PC (рекомендуется)**
```bash
# В PowerShell или WSL терминале на ПК:
wsl bash -c "pip install transformers torch llama-cpp-python"
wsl bash -c "cd /mnt/c/Users/AvA/Downloads/argos_scripts/mini-tron-50-model && python /path/to/convert_hf_to_gguf.py ./model --outfile mini-tron-50-f16.gguf --outtype f16"
```

**B) Использовать laptop** (linux уже скачал модель, но конвертация медленная)
- Нужен `convert_hf_to_gguf.py` + `transformers` + `torch`

**C) Дождаться поддержки NanoGPT в Ollama**
- Маловероятно в ближайшее время

**После конвертации:**
```bash
# Windows PowerShell:
cd C:\Users\AvA\Downloads\argos_scripts\mini-tron-50-model
ollama create argos-classic -f Modelfile
ollama run argos-classic
```

---

## 📋 Чеклист для пользователя (ручные действия)

- [ ] **Restart Obsidian** на ПК → включить Spaced Repetition в Community plugins
- [ ] **GGUF конвертация** mini-tron-50 (WSL или Python на ПК)
- [ ] **ollama create argos-classic** после получения GGUF
- [ ] **Протестировать**: `ollama run argos-classic` → спросить «Привет, классик!»
- [ ] **Добавить карточки** в существующие логи: выделить `==факты==` + `#flashcards`
- [ ] **Zigbee USB донгл** купить для Z2M
- [ ] **WiFi донгл** для Orange Pi

---

[[2026-05-14]]
[[ARGOS Spaced Repetition + Mini-Tron-50 Integration]]
[[Backbone Hub]]
