# 2026-05-14 — ARGOS Autopilot Day 2

## 🔧 Исправления

### 1. Cloudflare AI — ✅ Исправлен
- **Проблема**: `Cloudflaretoken` (неверное имя переменной) → `CLOUDFLARE_API_TOKEN`
- **Проблема**: отсутствовал `CLOUDFLARE_ACCOUNT_ID`
- **Проблема**: `ARGOS_DISABLE_CLOUDFLARE=1` блокировал провайдер
- **Исправлено**:
  - `CLOUDFLARE_API_TOKEN=cfat_xXNsM2sqJOz2r5ooYKIgEqWyIRpW2r2upKwJxyA25ad0c2ed`
  - `CLOUDFLARE_ACCOUNT_ID=19ee18f68337419570232f3529cccec3`
  - `ARGOS_DISABLE_CLOUDFLARE=0`
- **Тест**: Cloudflare AI (kimi-k2.5) отвечает успешно

### 2. P2P argos-pc heartbeat — ✅ Исправлен
- **Проблема**: argos-pc показывал "offline" в Brain API
- **Исправлено**: Добавлен `send_pc_heartbeat()` в ha_metrics_pusher.py
- **Результат**: Все 3 ноды online (argos-pc, argos-laptop, orangepi)

### 3. Orange Pi WiFi — ⚠️ Нет аппаратного WiFi
- **Проблема**: OPi One H3 не имеет встроенного WiFi
- **Подготовлено**: netplan, wpa_supplicant, udev, systemd для WiFi донгла
- **Требуется**: USB WiFi донгл

### 4. ESP8266 → ESPHome — ✅ Прошит и работает
- **Обнаружен**: ESP8266EX на /dev/ttyUSB0 (CH340, MAC 8c:4f:00:58:3a:fd, 4MB flash)
- **Прошит**: ESPHome 2026.4.5 конфигурация
- **WiFi**: Подключен к SiG (IP: 192.168.1.181)
- **MQTT**: Подключен к mosquitto на laptop (192.168.1.53:1883)
- **Датчики**: WiFi Signal, Uptime, ADC, Status
- **Home Assistant**: Автообнаружение через MQTT
- **P2P**: Зарегистрирован как argos-esp-bridge

### 5. Zigbee2MQTT — ✅ Исправлен (Node.js + debounce)
- **Проблема 1**: Node.js v18 слишком старый для Z2M 2.10.0
- **Исправлено**: Установлен Node.js v20.18.2 на OPi
- **Проблема 2**: debounce@3.0.0 — ESM модуль, несовместим с CJS
- **Исправлено**: Заменён ESM debounce на CJS шимм
- **Проблема 3**: Нет Zigbee координатора (ESP8266 теперь ESPHome)
- **Статус**: Z2M остановлен, ожидает Zigbee USB донгл

## 📊 Статус системы
| Нода | Статус | Роль |
|------|--------|------|
| argos-laptop | ✅ online | compute, ai, mcp |
| argos-pc | ✅ online | gpu, brain, ollama |
| orangepi | ✅ online | gpio, sensors, i2c |
| argos-esp-bridge | ✅ online | wifi, mqtt, sensors |

## 🤖 Провайдеры AI
| Провайдер | Статус |
|-----------|--------|
| Ollama (local) | ✅ 3 модели |
| Cloudflare AI | ✅ kimi-k2.5 |
| Ollama PC | ✅ 9 моделей |

## 📡 Сервисы
| Сервис | Порт | Статус |
|--------|------|--------|
| Brain API | 5001 | ✅ |
| MCP API | 8000 | ✅ |
| MQTT Broker | 1883 | ✅ |
| Ollama | 11434 | ✅ |
| HA Metrics | pusher | ✅ |
| Z2M | 8099 | ⏸️ нет Zigbee донгла |
| ESPHome | — | ✅ (ESP8266) |

## 🔜 Следующие шаги
- [ ] Добавить Zigbee USB координатор для Z2M
- [ ] Настроить Home Assistant интеграцию с ESPHome
- [ ] Добавить I2C датчики к ESP8266 (BME280/BMP280)
- [ ] WiFi донгл для Orange Pi
- [ ] Продолжить ARGOS Autopilot развитие

---

## 🖥️ ESP Display Manager & Brain Evolution — 17:00

### Что сделано:
- **ESP Display Manager** (`scripts/esp_display_manager.py`) — новый модуль
  - Отправляет статус системы на ESP32 OLED дисплей (192.168.1.211)
  - Обновляет каждые 30 сек: CPU, RAM, температура, кол-во нод P2P
  - LED цвет зависит от статуса: 🟢 все онлайн, 🟡 частично, 🔴 всё оффлайн, 🔵 нет данных
  - Автозапуск через `argos_start_all.sh`

- **Autonomous Brain** обновлён — добавлено отслеживание ESP устройств
  - `get_node_status()` теперь проверяет ESP32 Display и ESP8266 Bridge
  - Новые решения: `alert_esp_down` — алерт при потере ESP устройства
  - Отчёт в Obsidian теперь включает секцию ESP Devices

- **HA Metrics Pusher** обновлён — ESP мониторинг
  - `get_esp_status()` — проверяет доступность ESP32/ESP8266
  - Новые HA сенсоры: `sensor.argos_esp_devices`, `binary_sensor.argos_esp32_display`, `binary_sensor.argos_esp8266_bridge`

### P2P Network Status:
| Node | IP | Статус | Возможности |
|------|-----|--------|-------------|
| argos-laptop | 192.168.1.53:8000 | ✅ online | p2p, mcp, compute, ai |
| argos-pc | 192.168.1.66:5010 | ✅ online | gpu, compute, brain, ollama |
| orangepi | 192.168.2.168:7777 | ✅ online | gpio, i2c, sensors |
| argos-esp-bridge | 192.168.1.181 | ✅ online | wifi, mqtt, i2c, adc |
| argos-esp32-display | 192.168.1.211 | ✅ online | oled, led, display |

---

## 🧹 Vault Cleanup + Sync Fix + Integrations — 18:30

### 6. Удалены дубли в `.obsidian/` — ✅
- **Проблема**: `.obsidian/` содержал полную копию vault'а (2640 .md файлов)
  - `00 Memory Web`, `01 Projects`, `02 Logs`, `03 Memory`, `04 Project Mirror`, `05 SharedMemory Mirror`, `06 Link Stubs`, `07 Duplicates Archive`, `Daily`, `Excalidraw`
  - Все .md на корневом уровне, включая `.obsidian/.obsidian/`
- **Причина**: Баг в `sync-obsidian-memory.py` — не было исключения `.obsidian/` из списка файлов
- **Исправлено**:
  - Удалены ВСЕ дубли из `.obsidian/`
  - Сохранены только настоящие системные файлы: `plugins/` (Dataview, Templater, Excalidraw...) + `snippets/`
- **Защита от повторения**:
  - `sync-obsidian-memory.py` v2: `exclude = {".obsidian", ".git"}` + проверка `parts` на любой глубине
  - `vault-sync.sh` v2: функция `guard_path()` — fatal error если путь содержит `.obsidian/` или `.git/`
  - Lock-файл `/tmp/vault-sync.lock` — предотвращает concurrent runs

### 7. vault-sync.sh v2 — ✅
```bash
/home/ava/.local/bin/vault-sync.sh
```
- Path Guard: `guard_path()` отклоняет любые пути внутри `.obsidian/` или `.git/`
- Lockfile: `flock -n` — не запускает вторую копию
- Python sync: вызывает `sync-obsidian-memory.py` с `--quiet`

### 8. Spaced Repetition — «Вспомнить Всё» — ✅ Подготовлено
- **Плагин**: `obsidian-spaced-repetition` (st3v3nmw, 470K+ загрузок)
- **Скрипт установки**: `scripts/argos_install_spaced_repetition.sh`
  - Скачивает main.js, manifest.json, styles.css из latest release
  - Кладёт в `.obsidian/plugins/obsidian-spaced-repetition/`
- **Форматы карточек** (примеры из vault'а):
  - `Вопрос::Ответ` — single-line
  - `Вопрос?\nОтвет` — multi-line
  - `==cloze deletion==` — fill-in-the-blank
  - `192.168.1.66 ::: argos-pc` — reversed (bidirectional)
- **Тег**: `#flashcards` или `#flashcards/ARGOS`, `#flashcards/IoT`, `#flashcards/P2P`
- **Workflow**: читаешь лог → выделяешь `==ключевые факты==` → добавляешь `#flashcards` → повторяешь через Ctrl+P → "Review flashcards"
- **Заметка**: `[[ARGOS Spaced Repetition + Mini-Tron-50 Integration]]`

### 9. Mini-Tron-50 + русская классика — ✅ Подготовлено
- **Habr**: статья 1034858 — «Мини-Трон-50» (Imperius)
- **HuggingFace**:
  - `Imperius/mini-tron-50` — диалоговая LLM, 50M параметров
  - `Imperius/ru-classic` — датасет русской классики XIX–XX вв.
- **Codeberg**: парсер, BPE-токенайзер, NanoGPT-реализации (10M и 50M)
- **Скрипт**: `scripts/argos_setup_mini_tron.sh`
  - Скачивает модель через `huggingface-cli`
  - Конвертирует в GGUF (Q4_0) через llama.cpp
  - Создаёт `Modelfile` для Ollama
  - Регистрирует как `argos-classic`
- **System prompt**: «Ты — русскоязычный ассистент ARGOS, обученный на классической литературе. Говоришь ёмко, образно, с долей иронии.»
- **Интеграция в Brain API**: provider `argos-classic` → `http://localhost:11434`
- **ML Stack установлен** (13.05.2026):
  - pip: torch, transformers, opencv, ultralytics, spacy, nltk, xgboost, catboost, lightgbm, langchain, llamaindex...
  - pacman: python-pytorch, python-tensorflow, qemu-full, lxc, dnsmasq...
  - yay: python-langchain, waydroid...

---

## 🔜 Следующие шаги (обновлённые)
- [ ] Добавить Zigbee USB координатор для Z2M
- [ ] Настроить Home Assistant интеграцию с ESPHome
- [ ] Добавить I2C датчики к ESP8266 (BME280/BMP280)
- [ ] WiFi донгл для Orange Pi
- [ ] **Установить Spaced Repetition плагин** (`bash scripts/argos_install_spaced_repetition.sh`)
- [ ] **Протестировать mini-tron-50** (`bash scripts/argos_setup_mini_tron.sh` → `ollama run argos-classic`)
- [ ] **Добавить карточки** к существующим логам (ANDRAX, KolibriOS, P2P ноды)
- [ ] Продолжить ARGOS Autopilot развитие

---

## 🖥️ Hardware Deploy + Model Fallback — 19:35

### Windows PC Dongle (COM14)
- **Обнаружен:** USB Serial на COM14 (точный тип не определён удалённо)
- **Инструкция:** `devmgmt.msc` → COM14 → Hardware IDs для идентификации
- **Скрипт:** `windows_dongle_setup.ps1` + `windows_dongle.log` на ПК

### Orange Pi Hardware
- **Скрипт готов:** `orangepi_deploy_all.sh` (Z2M + ESPHome + UART)
- **Требуется:** запуск на Orange Pi после проверки `lsusb` + `ls /dev/ttyUSB*`

### Mini-Tron-50
- **GGUF конвертация:** ❌ Не удалась (кастомный tiktoken-BPE токенайзер, llama.cpp не поддерживает)
- **Решение:** Python API wrapper `argos_minitron_api.py` — OpenAI-compatible API на :11435
- **Статус:** Скрипт передан на Windows ПК

### Новые заметки
- `02 Logs/2026-05-14 Hardware Deploy — PC + OrangePi + Model Fallback.md`


---

## 🛠️ Финальная конфигурация — 20:00

### Настроено автоматически

| Компонент | Статус | Примечание |
|-----------|--------|------------|
| Vault cleanup + sync fix | ✅ | `.obsidian/` очищен, `vault-sync.sh` v2 |
| Spaced Repetition (Windows) | ✅ | Плагин установлен, ждёт enable в Obsidian |
| Mini-Tron-50 API (Windows) | ✅ | Python wrapper готов |
| Orange Pi ESP configs | ✅ | `esp-usb-sensor.yaml`, `esp32-usb-actuator.yaml` |
| Orange Pi auto-detect script | ✅ | `orangepi_auto_detect_and_start.sh` |

### Остающиеся блокеры

| Проблема | Причина | Решение |
|----------|---------|---------|
| **Zigbee донгл на Orange Pi** | Порт 3 хаба — `error -32` (STALL). Повторяется после всех сбросов. | Требуется физическое действие: проверить питание хаба или переткнуть донгл в порт 1 |
| **ESPHome на Orange Pi** | `pip install` падает из-за 500MB RAM | Нужно: `apt-get install esphome` или запуск с ноутбука |
| **Windows COM14** | Не могу опознать удалённо (encoding SSH → PowerShell) | Скрипт `windows_dongle_setup.ps1` на ПК для локального запуска |

### Следующие шаги (одно действие = всё запустится)

**Orange Pi:**
```bash
# Проверить питание хаба (должен гореть светодиод питания)
# Если не горит — подключить адаптер питания
# Если горит — переткнуть zigbee донгл в другой порт
# После этого:
lsusb  # должен показать 3+ устройства
```

**Windows:**
```powershell
# В PowerShell на ПК:
cd C:\Users\AvA\Downloads\argos_scripts
powershell -ExecutionPolicy Bypass -File windows_dongle_setup.ps1
# Затем restart Obsidian & enable Spaced Repetition
# Затем: python argos_minitron_api.py --preload
```


## ARGOS Integration Session — AutoGPT Entities & ESP

### Completed
- ✅ **AutoGPT Entity Framework** created (`src/auto_gpt_entities.py`)
  - 6 entities: DeepSeek, Kimi, Gemini, Cloudflare, Ollama Vision, MCP Server
  - MQTT heartbeat integration, status reporting, command handling
  - AgentBus updated with AI provider entities
- ✅ **ESP8266 firmware compiled** (514KB flash, 42% RAM)
- ✅ **ESP32 display config created** (`esphome/argos-esp32-display.yaml`)
  - SSD1306 OLED display, DHT22 sensor, MQTT message display
  - Compiling (in progress)
- ✅ **ESP MQTT Bridge** created (`src/esp_mqtt_bridge.py`)
  - 2 devices: ESP8266 bridge + ESP32 display
  - Display message API, device restart, sensor monitoring
- ✅ **Obsidian vault sync fixed** — broken symlink removed, all dirs sync OK
- ✅ **Vault sync to PC working** — All 4 dirs (argos, opencode, claude, shared) syncing
- ✅ **AutoGPT entity notes created** in Obsidian (5 provider entities)
- ✅ **AI Providers note updated** — 7/13 active providers documented
- ✅ **AgentBus expanded** — 12 agents now (6 original + 6 AI entities)

### In Progress
- 🔄 ESP32 firmware compiling
- 🔄 llava:7b model pulling on PC

### Issues
- ⚠️ HuggingFace token invalid (needs regeneration)
- ⚠️ ESP8266 offline (192.168.1.181 not responding to ping)
- ⚠️ ESP32 (192.168.1.211) online but running old firmware

---

## 🚀 ESP8266 Прошит — 20:45

### Большой успех: ttyUSB0 (ESP8266EX)
- **Прошивка:** `argos-esp-bridge` firmware (518 KB)
- **Результат:** Flash OK, hash verified, hard reset
- **MAC:** `8c:4f:00:58:3a:fd`
- **Драйвер:** CH340, порт ttyUSB0
- **Статус после прошивки:** ESP перезагружен, firmware запущен
- **WiFi:** Пытается подключиться к `SiG` (AP: `ARGOS-ESP-AP` как fallback)

### Автоматизация на Orange Pi
- **systemd service:** `argos-usb-autostart.service` — активен, ждёт zigbee
- **deploy script:** `orangepi_deploy_all.sh` — настроен

### Остаётся
- **ttyUSB1:** Второй ESP не прошился (не отвечает в bootloader). Нужен GPIO0 → GND
- **Zigbee:** Порт 3 хаба — аппаратный error -32. Нужно переткнуть в порт 1

