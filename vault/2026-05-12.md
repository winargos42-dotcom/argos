---
type: daily-note
date: 2026-05-12
tags: [daily, argos, status]
---

# 2026-05-12 — ARGOS Unified

## ✅ Выполнено сегодня

### 1. Импорты проверены
Все 5 модулей ARGOS импортируются корректно:
- `ArgosConsciousness`, `ArgosCuriosity`, `ArgosAwareness`
- `SelfSustainEngine`, `ThreeGPUManager`
- `src.core` и `main.py` загружаются без ошибок

### 2. Корень проекта почищен
223 сиротских .py файлов → `archive/root_py/`
Остались только ключевые: main.py, core.py, __init__.py + 14 используемых

### 3. Обсидиан объединён
```
Единая система:
  ARGOS vault (/Projects/argoss/vault/) = главный
  ├── F:\debug\аргос\ на ПК (синхронизация)
  ├── Obsidian Vault/ARGOS → симлинк
  ├── MyObsidianVault/ARGOS → симлинк
  └── SharedMemory → общая память

17GB дубликат Projects/argoss → заменён симлинком
```

### 4. 3D Graph настроен
- 16 цветовых групп (папки + теги)
- Цвета: Memory=оранжевый, Projects=голубой, SharedMemory=зелёный, Daily=фиолетовый
- CSS сниппет argos-graph.css (тёмный фон, свечение нод)
- Конфигурация синхронизирована на ПК

### 5. ПК = Сервер
- `SERVER.md` создан в vault
- GPU кластер включён: `OLLAMA_GPU_MODE=on`, `OLLAMA_GPU_ENABLED=true`
- GPU0 (RX580:8082) ✅, GPU2 (RX560:8084) ✅
- 3 мёртвых Docker контейнера удалены

### 6. Home Assistant автоматизации
4 автоматизации созданы:
- System Started notification
- GPU Server Down alert
- Sync Health Check
- Daily Status Report

ARGOS REST сенсоры: GPU0 Status, GPU2 Status
System Monitor: disk, memory, processor

### 7. Zigbee2MQTT
- Docker контейнер запущен (zigbee2mqtt)
- Mosquitto MQTT работает (исправлена ошибка persistence)
- Z2M config: ttyACM0, mqtt://localhost:1883
- ⚠️ Устройство ttyACM0 не подключено сейчас

## 📊 Текущий статус сервера (ПК)

| Сервис | Статус |
|--------|--------|
| Ollama | ✅ Работает |
| GPU0 (RX580:8082) | ✅ Healthy |
| GPU2 (RX560:8084) | ✅ Healthy |
| argos-redis | ✅ Up 23h |
| argos-brain | ✅ Healthy |
| argos-compute | ✅ Healthy |

## 📊 Текущий статус ноутбук

| Сервис | Статус |
|--------|--------|
| Home Assistant | ✅ :8123 |
| Mosquitto MQTT | ✅ :1883 |
| Z2M Docker | ✅ Running |
| Sync timers | ✅ 2min/5min |
